name: myservedemo

on: workflow_dispatch

jobs:
  job_1:
    name: Add 3 and 7
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: |
          expr 3 + 7 > math-homework.txt
      - name: Upload math result for job 1
        uses: actions/upload-artifact@v3
        with:
          name: homework
          path: math-homework.txt

  job_2:
    name: print result
    runs-on: macos-13
    steps:
      - run: sleep 20
      - name: Download math result for job 1
        uses: actions/download-artifact@v3
        with:
          name: homework
      - shell: bash
        run: |
          #curl -L -o ./gofiledownload.js https://gist.githubusercontent.com/fawazahmed0/5a779ddebfb614bf099e1c004c2dacb4/raw/26e51b18224532834a954b2462ecb1cbb9541d9e/gofiledownload.js
          #npm i playwright
          #npx playwright install --with-deps
          #node gofiledownload.js

      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3

      - shell: bash
        run: |
          sleep 36000
